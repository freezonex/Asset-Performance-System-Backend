spring:
  datasource:
    url: jdbc:mysql://${SUPOS_HOSTED_HOST:lake-sql-mariadb-95c7b626dc.default.svc}:${SUPOS_HOSTED_PORT:3306}/${SUPOS_HOSTED_DBNAME:apsdatabase}?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC&socketTimeout=30000
    username: ${SUPOS_HOSTED_USERNAME:supos_549bed47}
    password: ${SUPOS_HOSTED_PASSWORD:d18391335b2750}
  sql:
    init:
      schema-locations: classpath:sql/schema.sql
#      data-locations: classpath:sql/data.sql
      mode: always
      continue-on-error: true

logging:
  level:
    root: info
    com.supos.assets: debug